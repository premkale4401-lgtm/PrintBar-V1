# PrintBar Backend — Alembic Configuration
# All database migration settings are managed here.
# Connection string is always read from the DATABASE_URL environment variable.

[alembic]
script_location = alembic
prepend_sys_path = .
version_path_separator = os

# The database URL is injected at runtime from the environment.
# Never hardcode a connection string here.
sqlalchemy.url = %(DATABASE_URL)s

[post_write_hooks]

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
